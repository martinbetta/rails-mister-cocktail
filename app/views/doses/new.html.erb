<%= simple_form_for([@cocktail, @dose]) do |f| %> <!-- Why both, how can i realized -->
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
